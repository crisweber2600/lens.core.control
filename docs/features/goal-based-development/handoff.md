# Handoff - Goal Based Development

## Recommended Next Workflow

`/preplan`

## Why This Route

The request is broad and not yet implementation-ready. Full-track PrePlan should discover what goal based development means, which users and lifecycle phases it affects, and what artifacts or command behavior need to change.

## Context To Carry Forward

- Raw user intent: "goal based development".
- Feature ID: `goal-based-development`.
- Work archive: `docs/features/goal-based-development/`.
- Current phase: `preplan`.
- Current track: `full`.
- Ownership and target repositories are intentionally unresolved.
- No direct related work was confirmed during intake.

## Inputs Needed By PrePlan

- The user's concrete examples of development work that should be goal based.
- Desired goal lifecycle: capture, refinement, approval, story linkage, dev execution, validation, completion, reporting.
- Whether goals should be represented as metadata, a standalone artifact, a story section, lifecycle gates, or command behavior.
- Candidate target surfaces after discovery: Lens lifecycle orchestration, work intake, story generation, dev execution, reporting, or documentation.

## Open Questions

- What problem should goal based development solve first?
- Who uses the goal model: operator, planner, developer, reviewer, stakeholder, or all of them?
- What evidence proves a development effort satisfied its goals?
- Should goals be immutable after intake, explicitly refined through gates, or both?
