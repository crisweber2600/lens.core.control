---
feature: nextlens-src-bottomup
story_id: E2-S2
doc_type: story
status: done
title: "Implement Packet Schema Fixtures and Read-Only Validator"
depends_on:
  - E2-S1
updated_at: 2026-05-20T00:00:00Z
epic_id: E2
epic_title: "Read-Only Validation and Non-Effects Proof"
target_repo: NextLens
target_repo_path: TargetProjects/nextlens/src/NextLens
---

# Story E2-S2: Implement Packet Schema Fixtures and Read-Only Validator

Status: done

## Story

As an operator, I want packet validation to run without mutation, so that I can trust packet validity before any write.

## Implementation Context

- Dev implementation target: `NextLens` at `TargetProjects/nextlens/src/NextLens`.
- Implementation writes are allowed only under `TargetProjects/nextlens/src/NextLens`.
- Runtime validation is read-only unless optional reports write under configured `reports_output_path`.
- Implement the validator from the handwritten contract locked in E2-S1.

## Acceptance Criteria

1. Given `bul-validate-packet` receives a packet or draft path, when validation runs, then it enforces schema version, `sourceMode=bottom_up`, required identity, selected feature, scope, constraints, assumptions, provenance, receipt reference, and null/unpromoted topology fields.
2. Given a valid packet fixture, when validation runs, then it returns `packetValid.status=pass`, preserves topology null/unpromoted, and writes nothing.
3. Given invalid fixtures for multi-candidate, missing included scope, missing explicit out-of-scope, missing non-inference rules, promoted assumptions, invalid source mode, unsupported schema version, or branch/editor/cwd inference, when validation runs, then it returns `status=fail` with structured errors.
4. Given validation runs with `--report`, when a report is requested, then any report is written only under `reports_output_path` and never modifies the packet under inspection.
5. Given repository state is compared before and after read-only validation, when validation completes, then no forbidden file changed.

## Tasks / Subtasks

- [ ] Implement `bul-validate-packet/scripts/validate_packet.py` using handwritten rules.
- [ ] Add valid and invalid packet fixtures.
- [ ] Return JSON with `packetValid`, `bmadReady`, `hardBlockers`, and `advisories` where appropriate.
- [ ] Add no-mutation tests for validation and optional report behavior.

## Dependencies And Sequencing

- Depends on: E2-S1.
- This story must finish before BMAD readiness separation, candidate draft validation, and create workflow writes depend on validation output.
- It feeds golden examples and evals in E4-S2 and E4-S3.

## Dev Notes

- Use canonical JSON packet artifacts with `schemaVersion: bul.feature-packet.v1`, `sourceMode: bottom_up`, lower camelCase fields, and null/unpromoted topology.
- Distinguish intentionally unknown values from missing or empty required values.
- Validation must not infer identity from branch name, open editor file, or current working directory.

## Test / Validation Expectations

- Unit tests for every validator rule and structured error.
- Golden fixture tests for valid, invalid multi-candidate, missing out-of-scope, unsupported schema version, and promoted topology.
- No-mutation test around validation and optional report behavior.

## Non-Lens Negative Acceptance Criteria

- Do not write or require `feature.yaml`, governance publish, direct governance repo writes, governance docs mirror writes, Lens branch topology, Lens constitution runtime, release clones, `.github`, Lens control-repo implementation paths, current NextLens top-down runtime modules, Landscape, Derived Graph, Salmon, promotion, adjacency, pressure, roadmap, or service/domain/program truth paths.

## Source References

- `docs/nextlens/src/nextlens-src-bottomup/epics.md`: Story E2-S2; FR33-FR42; M2 carry-forward.
- `docs/nextlens/src/nextlens-src-bottomup/stories.md`: E2-S2 scope, acceptance criteria, and validation expectations.
- `docs/nextlens/src/nextlens-src-bottomup/architecture.md`: Packet Artifact; Validation Architecture; Validation Result Shape.
- `docs/nextlens/src/nextlens-src-bottomup/finalizeplan-review.md`: M1, M2, and M3 sequencing requirements.

## Dev Agent Record

### Agent Model Used

cx/gpt-5.5-xhigh

### Debug Log References
- 2026-05-20: `d:/lensTrees/Lens.Core.control/.venv/Scripts/python.exe -m pytest tests -q` from `TargetProjects/nextlens/src/NextLens` -> 34 passed.
- 2026-05-20: `d:/lensTrees/Lens.Core.control/.venv/Scripts/python.exe skills/bul-create-packet/scripts/validate_module.py --repo-root . --run-tests` from `TargetProjects/nextlens/src/NextLens` -> status pass; nested tests 34 passed.
### Completion Notes List
- Implemented remaining Bottom-Up LENS module stories E1-S2 through E4-S4 as an integrated target-repo pass.
- Added setup registration/idempotency coverage, handwritten packet validation/readiness separation, path guard, receipt verification, create workflow primitives, docs, evals, fixtures, release checklist, and module validation.
- Target commit: `f577e56` on `feature/bottomup`; target PR: https://github.com/crisweber2600/NextLens/pull/12.
### File List
- `.claude-plugin/marketplace.json`
- `README.md`
- `docs/commands.md`
- `docs/release-readiness.md`
- `evals/**`
- `skills/bul-create-packet/**`
- `skills/bul-validate-packet/**`
- `skills/bul-verify-receipt/**`
- `tests/**`
