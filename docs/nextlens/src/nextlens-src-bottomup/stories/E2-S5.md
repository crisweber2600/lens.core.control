---
feature: nextlens-src-bottomup
story_id: E2-S5
doc_type: story
status: ready-for-dev
title: "Implement Receipt and Run-Metadata Verification"
depends_on:
  - E2-S4
updated_at: 2026-05-20T00:00:00Z
epic_id: E2
epic_title: "Read-Only Validation and Non-Effects Proof"
target_repo: NextLens
target_repo_path: TargetProjects/nextlens/src/NextLens
---

# Story E2-S5: Implement Receipt and Run-Metadata Verification

Status: ready-for-dev

## Story

As a support user, I want receipt claims verified against run metadata and changed files, so that false non-effects claims are hard failures.

## Implementation Context

- Dev implementation target: `NextLens` at `TargetProjects/nextlens/src/NextLens`.
- Implementation writes are allowed only under `TargetProjects/nextlens/src/NextLens`.
- Runtime verification is read-only unless optional reports write under configured `reports_output_path`.
- This verifier must exist before packet creation can claim accepted-packet success.

## Acceptance Criteria

1. Given `bul-verify-receipt` receives a receipt and run metadata, when verification runs, then it compares `writtenFiles`, `changedFiles`, and `nonEffects` claims against forbidden path categories.
2. Given a valid receipt and matching run metadata, when verification runs, then it returns `status=pass` and `Non-effects verified` with checked-file summary.
3. Given a false-receipt fixture claims no graph update but changed files include a graph path, when verification runs, then it returns `status=fail`, labels the run invalid, reports `Receipt mismatch detected`, and includes violated category plus changed-file evidence.
4. Given required metadata is missing, when verification runs, then it fails closed and writes no accepted verification result outside optional report scope.
5. Given create workflow integration is attempted, when this verifier is absent or failing, then the create workflow cannot claim accepted-packet success.

## Tasks / Subtasks

- [ ] Implement `bul-verify-receipt/scripts/verify_receipt.py`.
- [ ] Verify receipt claims against run metadata, `writtenFiles`, and `changedFiles`.
- [ ] Add false-receipt and forbidden-changed-file fixtures before create success depends on verification.
- [ ] Add optional report behavior guarded by `reports_output_path`.

## Dependencies And Sequencing

- Depends on: E2-S4.
- E3-S4 depends on this story before packet creation may claim accepted success.
- E4-S2 reuses false-receipt and forbidden-write examples from this verification contract.

## Dev Notes

- Receipt mismatch is invalid, not warning-only.
- Run metadata must not dump environment variables, secrets, credentials, or tokens.
- Verification should scale with changed-file manifests rather than repository history scans.

## Test / Validation Expectations

- Unit tests for valid receipt, false receipt, missing metadata, forbidden changed file, and optional report path guard.
- Fixture for `false-graph-receipt.json` and a forbidden-write changed-file manifest.
- Repeatable command expectation for verification tests.

## Non-Lens Negative Acceptance Criteria

- Do not write or require `feature.yaml`, governance publish, direct governance repo writes, governance docs mirror writes, Lens branch topology, Lens constitution runtime, release clones, `.github`, Lens control-repo implementation paths, current NextLens top-down runtime modules, Landscape, Derived Graph, Salmon, promotion, adjacency, pressure, roadmap, or service/domain/program truth paths.

## Source References

- `docs/nextlens/src/nextlens-src-bottomup/epics.md`: Story E2-S5; FR43-FR48; M3 carry-forward.
- `docs/nextlens/src/nextlens-src-bottomup/stories.md`: E2-S5 scope, acceptance criteria, and validation expectations.
- `docs/nextlens/src/nextlens-src-bottomup/architecture.md`: Receipt Artifact; Run Metadata; Receipt verification; Read-Only Skill Pattern.
- `docs/nextlens/src/nextlens-src-bottomup/finalizeplan-review.md`: M3 and party-mode QA challenge.

## Dev Agent Record

### Agent Model Used

TBD by dev agent.

### Debug Log References

### Completion Notes List

### File List
