---
feature: nextlens-src-bottomup
story_id: E2-S1
doc_type: story
status: done
title: "Lock Handwritten Python Packet Validation Contract"
depends_on:
  - E1-S1
updated_at: 2026-05-20T00:00:00Z
epic_id: E2
epic_title: "Read-Only Validation and Non-Effects Proof"
target_repo: NextLens
target_repo_path: TargetProjects/nextlens/src/NextLens
---

# Story E2-S1: Lock Handwritten Python Packet Validation Contract

Status: done

## Story

As a validator maintainer, I want the MVP validation mechanism locked to handwritten Python rules, so that schema behavior is concrete before tests and packet workflows depend on it.

## Implementation Context

- Dev implementation target: `NextLens` at `TargetProjects/nextlens/src/NextLens`.
- Implementation writes are allowed only under `TargetProjects/nextlens/src/NextLens`.
- Runtime validation is read-only unless a later `--report` path writes under configured `reports_output_path`.
- This story locks the validation contract before schema fixtures, readiness checks, path guard, receipt verification, and create workflow work depend on it.

## Acceptance Criteria

1. Given the first validator story is implemented, when maintainers inspect validator design docs and script stubs, then MVP validation uses handwritten Python standard-library-first rules and does not add a `jsonschema` dependency.
2. Given future schema interoperability is discussed, when the validator contract is read, then `jsonschema` remains a future option only after MVP handwritten validation passes fixtures.
3. Given rule categories are listed, when maintainers inspect the contract, then schema version, source mode, selected feature, scope, constraints, assumptions, provenance, receipt reference, topology, and non-effects requirements are covered.
4. Given downstream work attempts to write schema tests or create workflow logic, when dependencies are checked, then this story is required first.

## Tasks / Subtasks

- [ ] Document the handwritten Python validation contract and rule inventory.
- [ ] Stub validator result shapes and structured error shape.
- [ ] Confirm no `jsonschema` import or dependency is introduced.
- [ ] Add tests or snapshots for rule inventory and structured errors.

## Dependencies And Sequencing

- Depends on: E1-S1.
- E2-S2, E2-S3, E2-S4, and create workflow stories depend on this contract.
- Do not implement full packet validation fixtures before this story locks the mechanism and error shape.

## Dev Notes

- Supported Python baseline is Python 3.11+.
- Use standard library first. PEP 723 dependencies are allowed only when standard library cannot satisfy a later requirement; this story specifically forbids adding `jsonschema` for MVP.
- Use lower camelCase JSON result fields and structured errors with `code`, `field`, `message`, and `recommendation`.

## Test / Validation Expectations

- Dependency check confirming no `jsonschema` import or script dependency.
- Validator rule inventory test or snapshot.
- Structured error-shape unit test.

## Non-Lens Negative Acceptance Criteria

- Do not write or require `feature.yaml`, governance publish, direct governance repo writes, governance docs mirror writes, Lens branch topology, Lens constitution runtime, release clones, `.github`, Lens control-repo implementation paths, current NextLens top-down runtime modules, Landscape, Derived Graph, Salmon, promotion, adjacency, pressure, roadmap, or service/domain/program truth paths.

## Source References

- `docs/nextlens/src/nextlens-src-bottomup/epics.md`: Story E2-S1; M1 carry-forward; Technology and Dependency Baseline.
- `docs/nextlens/src/nextlens-src-bottomup/stories.md`: E2-S1 scope, acceptance criteria, and validation expectations.
- `docs/nextlens/src/nextlens-src-bottomup/architecture.md`: Technology and Dependency Baseline; Validation Architecture; Gap Analysis Results.
- `docs/nextlens/src/nextlens-src-bottomup/finalizeplan-review.md`: M1 validation mechanism requirement.

## Dev Agent Record

### Agent Model Used

cx/gpt-5.5-xhigh

### Debug Log References
- 2026-05-20: `d:/lensTrees/Lens.Core.control/.venv/Scripts/python.exe -m pytest tests -q` from `TargetProjects/nextlens/src/NextLens` -> 37 passed.
- 2026-05-20: `d:/lensTrees/Lens.Core.control/.venv/Scripts/python.exe skills/bul-create-packet/scripts/validate_module.py --repo-root . --run-tests` from `TargetProjects/nextlens/src/NextLens` -> status pass; nested tests 37 passed; artifact evals 8 passed.
### Completion Notes List
- Implemented remaining Bottom-Up LENS module stories E1-S2 through E4-S4 as an integrated target-repo pass.
- Added setup registration/idempotency coverage, handwritten packet validation/readiness separation, path guard, receipt verification, create workflow primitives, docs, evals, fixtures, release checklist, and module validation.
- Target commits: `f577e56`, `2897f73` on `feature/bottomup`; target PR: https://github.com/crisweber2600/NextLens/pull/12.
### File List
- `.claude-plugin/marketplace.json`
- `README.md`
- `docs/commands.md`
- `docs/release-readiness.md`
- `evals/**`
- `skills/bul-create-packet/**`
- `skills/bul-validate-packet/**`
- `skills/bul-verify-receipt/**`
- `tests/**`
